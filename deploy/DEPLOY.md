# 部署指南

主文档已移至项目根目录：[../DEPLOY.md](../DEPLOY.md)

## 快速参考

### 云端启动
```bash
cd ~/.openclaw/workspace/skills/mobile-rpa
bash deploy/start-all.sh
```

### 本地启动（Midscene + frp 隧道）
```cmd
deploy\start-local-to-cloud.cmd
```

### 本地纯模拟器调试（不连云端）
```cmd
deploy\start-emulator-midscene.cmd
```
